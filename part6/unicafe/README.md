# Unicafe (Part 6) — Zustand version

This is a minimal Unicafe implementation using Zustand for state management.

To run:

```bash
cd part6/unicafe
npm install
npm run dev
```

Open http://localhost:5173
