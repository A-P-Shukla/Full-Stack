# Anecdotes (Part 6) — Zustand version

This is an implementation of the anecdotes app using Zustand for state management.

Run:

```bash
cd part6/zustand-anecdotes
npm install
npm run dev
```

Open http://localhost:5173
